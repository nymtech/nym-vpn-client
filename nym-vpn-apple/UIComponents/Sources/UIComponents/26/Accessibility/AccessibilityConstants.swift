import CoreFoundation

/// Constants for building accessible UI components.
public enum AccessibilityConstants {
    /// Minimum interactive area dimensions per Apple's Human Interface Guidelines.
    public enum MinTapTarget {
        public static let size: CGFloat = 44
    }
}
