UI:

- handle first request (it fails)
- handle case when exit nodes changes while proxy is running
- does not work when VPN is running
- does it work in other langs?
- fix node selection, sometimes it gets nodes not supporting socks5
- adjust default timeouts
