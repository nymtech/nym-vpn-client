name: ci-nym-vpn-cli

on:
  # push:
  pull_request:
    paths:
      - "nym-vpn-cli/**"
      - "nym-vpn-lib/**"
      - "wireguard/**"
      - "Cargo.toml"
      - ".github/workflows/ci-nym-vpn-cli.yml"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  AGENT_ISSELFHOSTED: 1 # https://github.com/actions/setup-go/issues/432

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # os: [ubuntu-22.04, custom-runner-mac-m1, windows11]
        os: [windows11]
    runs-on: ${{ matrix.os }}

    steps:
      - name: "Cleanup working directory"
        if: contains(matrix.os, 'custom')
        run: |
          ls -la ./
          rm -rf ./* || true
          rm -rf ./.??* || true
          ls -la ./

      - name: Install system dependencies
        if: matrix.os == 'ubuntu-22.04'
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev libmnl-dev libnftnl-dev protobuf-compiler git curl gcc g++ make

      - name: Support longpaths on windows
        if: contains(matrix.os, 'windows')
        run: git config --system core.longpaths true

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install rust toolchain
        id: rust-toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ steps.rust-toolchain.outputs.cachekey }}
          workspaces: |
            nym-vpn-cli

      - name: Set env
        shell: bash
        run: |
          if ${{ contains(matrix.os, 'ubuntu') || matrix.os == 'ubuntu-22.04' }}; then
            triplet=x86_64-unknown-linux-gnu
          elif ${{ matrix.os == 'macos-12' || matrix.os == 'macos-13' }}; then
            triplet=x86_64-apple-darwin
          elif ${{ contains(matrix.os, 'macos') || contains(matrix.os, 'mac-m1') }}; then
            triplet=universal-apple-darwin
          elif ${{ contains(matrix.os, 'windows') }}; then
            triplet=x86_64-pc-windows-msvc
          else
            echo " ✗ unknown platform/arch [${{ matrix.os }}]"
            exit 1
          fi
          echo "TRIPLET=$triplet" >> $GITHUB_ENV
          echo "RUSTFLAGS=-L ${GITHUB_WORKSPACE}/build/lib/${triplet}" >> $GITHUB_ENV

      - name: Set extra env for Windows
        if: contains(matrix.os, 'windows')
        shell: bash
        run: |
          echo "RUSTFLAGS=-L ${GITHUB_WORKSPACE}/build/lib/${triplet} -Clink-args=/LIBPATH:${GITHUB_WORKSPACE}/build/lib/x64-Debug" >> $GITHUB_ENV
          mkdir -p ${GITHUB_WORKSPACE}/build/lib/${triplet}/

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: "stable"

      - name: Install Protoc
        uses: arduino/setup-protoc@v2
        if: contains(matrix.os, 'macos') || contains(matrix.os, 'windows')
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup MSBuild.exe
        if: contains(matrix.os, 'windows')
        uses: microsoft/setup-msbuild@v2

      # Needs to come before building mullvad libs, since it creates the output folder
      # DO NOT SPECIFY `shell: bash` because that spawns a process within WSL rather than using
      # powershells `bash`. We need Powershell `bash` because that shells out to GitBash.
      - name: Build wireguard
        shell: bash
        run: |
          ./wireguard/build-wireguard-go.sh

      - name: Checkout mullvad libs for Windows
        if: contains(matrix.os, 'windows')
        uses: actions/checkout@v4
        with:
          repository: nymtech/nym-vpn-mullvad-libs
          path: "nym-vpn-mullvad-libs"
          submodules: true

      - name: Build winfw.dll from mullvad
        if: contains(matrix.os, 'windows')
        shell: bash
        run: |
          cd ${GITHUB_WORKSPACE}/nym-vpn-mullvad-libs
          ./build-windows-modules.sh

      - name: Move winfw.dll to build directory
        if: contains(matrix.os, 'windows')
        shell: bash
        run: |
          mv ${GITHUB_WORKSPACE}/nym-vpn-mullvad-libs/windows/winfw/bin/x64-Debug ${GITHUB_WORKSPACE}/build/lib/

      - name: Download wintun.zip
        if: contains(matrix.os, 'windows')
        shell: bash
        run: |
          curl --output ${GITHUB_WORKSPACE}/wintun.zip https://www.wintun.net/builds/wintun-0.14.1.zip

      - name: Unzip wintun.zip
        if: contains(matrix.os, 'windows')
        shell: bash
        run: |
          unzip ${GITHUB_WORKSPACE}/wintun.zip

      - name: Move wintun.dll to build directory
        if: contains(matrix.os, 'windows')
        shell: bash
        run: |
          mv ${GITHUB_WORKSPACE}/wintun/bin/amd64/wintun.dll ${GITHUB_WORKSPACE}/build/lib/

      - name: rustfmt check
        run: cargo fmt --check

      - name: Build
        run: cargo build --verbose

      - name: Run tests
        run: cargo test --verbose

      - name: Clippy
        run: cargo clippy -- -Dwarnings
