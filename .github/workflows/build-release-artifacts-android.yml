name: build-release-artifacts-android

on:
  workflow_call:
    inputs:
      release_type:
        description: "Release type (release, prerelease, nightly)"
        type: string
        required: true
      version:
        description: "Orchestrated app version to embed (empty = use Constants.kt as before)"
        type: string
        required: true
      play:
        description: "Google Play"
        type: boolean
        default: false
      fdroid:
        description: "F-Droid"
        type: boolean
        default: false
    outputs:
      fdroid_artifact_name:
        description: "Name of the F-Droid artifact"
        value: ${{ jobs.build-android-fdroid.outputs.artifact_name }}
      play_artifact_name:
        description: "Name of the Google Play artifact"
        value: ${{ jobs.build-android-play.outputs.artifact_name }}

permissions:
  contents: read

jobs:
  inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "release_type: ${{ inputs.release_type }}"
          echo "version: ${{ inputs.version }}"
          echo "play: ${{ inputs.play }}"
          echo "fdroid: ${{ inputs.fdroid }}"

  build-android-fdroid:
    name: F-Droid build
    if: ${{ inputs.fdroid == 'true' }}
    uses: ./.github/workflows/build-nym-vpn-android.yml
    secrets: inherit
    with:
      build_type: ${{ inputs.release_type }}
      build_format: apk
      build_flavor: fdroid
      version: ${{ inputs.version }}

  build-android-play:
    name: Google Play build
    if: ${{ inputs.play == 'true' }}
    uses: ./.github/workflows/build-nym-vpn-android.yml
    secrets: inherit
    with:
      build_type: ${{ inputs.release_type }}
      build_format: aab
      build_flavor: play
      version: ${{ inputs.version }}
